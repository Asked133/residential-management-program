
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Unit Tests - Vivienda Detalle Screen', () {
    test('Verificar formato de deudas y morosidad (Unitario)', () {
      expect(true, isTrue);
    });
  });

  group('Widget Tests - Vivienda Detalle Screen', () {
    testWidgets('Renderiza detalles de la casa seleccionada (Widget)', (WidgetTester tester) async {
      // await tester.pumpWidget(MaterialApp(home: ViviendaDetalleScreen()));
    });
  });
}

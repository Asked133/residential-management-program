import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Unit Tests - Residentes List Screen', () {
    test('Verificar filtrado de residentes (Unitario)', () {
      expect(true, isTrue);
    });
  });

  group('Widget Tests - Residentes List Screen', () {
    testWidgets('Renderiza ListView de residentes (Widget)', (WidgetTester tester) async {
      // await tester.pumpWidget(MaterialApp(home: ResidentesList()));
    });
  });
}
